import React from 'react'
// import Sound from 'react-sound'
import OceanSound from '../../assets/mp3/ocean.mp3'

// const Audio = props => {
  // return(
    // <Sound {...props} url='../../assets/mp3/ocean' playStatus={Sound.status.PLAYING} />
    const Audio = (tSc) => {
      // this.myRef = React.createRef();
      // if(tSc === timerStates.COMPLETE)
       return (
        <audio src='../../assets/mp3/ocean.mp3' autoPlay/>
       )
     }
  // )
// }

export default Audio